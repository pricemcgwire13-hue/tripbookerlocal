# Install npm packages
- `npm install`

# Run vite instance
- `npm run dev`

# Pages (Make sure to have nothing else running on port 5173)
- Home: `http://localhost:5173`
- SearchResult: `http://localhost:5173/searchresult`
- Checkout: `http://localhost:5173/checkout`
- Error Example: `http://localhost:5173/errorexample`






# assignment 11

# spinner loading states on buttons 'home, search, checkout, submit' on home page. Spinner on profile button in header. spinner on search in search page

# small animations when hovering over 'hotel, flight, rental, cruise' on home page. Hover states also added throughout website buttons. on checkout page added small enlarge animation when hovering over sections of the page. fade ins on all pages

# when clicking "complete secure booking" an error message is displayed on information not yet filled out

# select state on checkout shows all states and is clickable

# search with suggestions on search page 

# Assignment 12

Our form we worked on for this assignment 12 is the CHECKOUT PAGE . We have added all the assignment details expected from us incliduing fixing of previous asignment 10 and 11 comments. 

Addressing the comment, "Mobile design patterns are different from mobile apps. Mobile patterns use icons over text label signifiers because of limited screen space. Currently, the desktop elements seem cramped on the mobile screen rather than being adapted for a mobile-first layout, which doesn't follow responsive design best practices," from Assignment 10: during our usability study, our users were having a hard time understanding what each icon present on the home screen represented. Therefore, we had to add text, and it now looks different from our wireframes.

Our current system does not match the wireframes since we have developed it to become higher fidelity, as mentioned during the code review by one of the TAs. We will continue to keep our promised features and functionality the same as mentioned and demonstrated in the original Figma wireframes created during the semester. We have also discussed this matter with Dr. Lipsa. Therefore, we are hoping we do not receive a penalty for these situations.

# Other fixed features from previous assignment comments addressed:
  Our system now is developed in a higher fidelity, therefore does not math figma wireframes as discussed above. 
  We added scrolling on the checkout page and other pages as needed. 
  Fixed cards on the serach results screen.
  All our buttons on the homescreen now lead to diffewrent things just as dropdowns for the Location and number of travelers, and a calender for the dates button. 
  We added padding between the drop down result list.
  We used loading spinner to signify search loading.
  We took the dropdown list outside of  the search field so it overlays the page rather than expanding the input box.
  We have added  "No results found" below the search field.
  We have added active state that was missing on one layout.
